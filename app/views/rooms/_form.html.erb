<%= form_with(model: room, local: true) do |f| %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control"%>
  </div>

  <a><%= f.submit "Add room", class: "btn btn-primary mt-0"%></a>

<% end %>